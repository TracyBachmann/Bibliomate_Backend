# ---- Build & Registry ----
REGISTRY_NS=your-docker-namespace
TAG=latest

# ---- ASP.NET Core ----
# Pour le dev local, tu peux mettre "Development".
ASPNETCORE_ENVIRONMENT=Production

# ---- SQL Server ----
SA_PASSWORD=ChangeMe_UseAStrongPassword123!
# Exemple de chaîne de connexion (adapte host/DB/params)
CONNECTIONSTRINGS__DEFAULTCONNECTION=Server=sql;Database=BiblioMate;User ID=sa;Password=${SA_PASSWORD};TrustServerCertificate=true;MultipleActiveResultSets=true

# ---- MongoDB ----
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=ChangeMe_RootMongoPwd!
# Chaîne de connexion appli (utilise un user applicatif dédié si possible)
MONGODB__CONNECTIONSTRING=mongodb://root:${MONGO_INITDB_ROOT_PASSWORD}@mongo:27017/bibliomate?authSource=admin

# ---- Auth / JWT ----
# Clé secrète forte (≥32 chars). Ex: openssl rand -base64 48
JWT__KEY=ChangeMe_SuperSecretJwtKey_AtLeast32Chars

# ---- Email (SendGrid) ----
SENDGRID__APIKEY=
SENDGRID__FROMEMAIL=no-reply@yourdomain.tld
SENDGRID__FROMNAME=BiblioMate

# ---- Email (SMTP alternatif) ----
SMTP__HOST=smtp.yourprovider.tld
SMTP__PORT=587
SMTP__USERNAME=no-reply@yourdomain.tld
SMTP__PASSWORD=ChangeMe_SmtpPwd!
SMTP__FROMEMAIL=no-reply@yourdomain.tld
SMTP__FROMNAME=BiblioMate

# ---- Intégrations externes ----
GOOGLEBOOKS__APIKEY=

# ---- Chiffrement applicatif ----
# Clé de chiffrement app (ex: 32 bytes base64)
ENCRYPTION__KEY=ChangeMe_32BytesBase64OrHex
